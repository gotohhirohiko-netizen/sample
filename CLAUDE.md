# リポジトリ運用ルール

## バージョン管理・変更履歴(必須)

アプリ(`web/`)に変更を加えて完了したら、**毎回必ず**以下を行う:

1. `web/package.json` の `version` を更新する(セマンティックバージョニング、`x.y.z`)
   - **z(下一桁)**: 不具合修正
   - **y(二桁目)**: 機能変更・追加
   - **x(一桁目)**: 大幅な変更(アーキテクチャ変更等)
   - 上位桁を上げた場合は下位桁を0にリセットする(例: 1.2.3 → 機能追加 → 1.3.0)
2. `web/CHANGELOG.md` に変更内容のエントリを追加する(`## [x.y.z] - YYYY-MM-DD` 形式、日本語で内容を簡潔に記述)

このバージョンはビルド時に `web/vite.config.ts` の `__APP_VERSION__` 経由でアプリ内(設定画面の「アプリ情報」)にも表示されるため、変更のたびに必ず更新すること。`web/CHANGELOG.md` はアプリ内(設定 > 変更履歴)でも `?raw` importでそのまま表示されるので、単なるメモ書きではなく利用者が読む前提の文章にすること。単なるドキュメント修正やテストスクリプトの調整など、アプリの動作に影響しない変更ではバージョンを上げなくてよい。
